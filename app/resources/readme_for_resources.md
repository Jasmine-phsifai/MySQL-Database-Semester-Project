# readme_for_resources

UI 静态资源 (M2 填充).

预留:
- `theme.qss` — 在 qdarktheme 之上叠加的细节样式 (表头加粗、横幅圆角等)
- `icons/` — 工具栏图标 (新增 / 删除 / 保存 / 刷新 / 导入 / 导出 / 备份)
- `mermaid_template.html` — 内嵌 mermaid live 渲染 (可选)

调用方: `ui/main_window.py`, `ui/widgets/editable_table.py`.
